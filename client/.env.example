# Aaron Jersey World — Client Environment Variables

# Backend API base URL (no trailing slash)
# Dev: use /api/v1 (Vite proxy). Production: your full API URL e.g. https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1

# WhatsApp number for wa.me links (digits only, country code included)
VITE_WHATSAPP_NUMBER=256781161690

# Google Maps embed URL for contact/location sections
VITE_GOOGLE_MAPS_EMBED_URL=
